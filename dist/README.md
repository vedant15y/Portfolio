# Vedant Portfolio

Static portfolio build for `RESUME.EXE`.

## Deploy

Push the contents of this folder to a GitHub repository:

- `index.html`
- `Resume.pdf`
- `assets/`

For GitHub Pages, enable Pages from the repository settings and deploy from the `main` branch root.

For Netlify or Vercel, import the repository and use the project root as the publish directory.
